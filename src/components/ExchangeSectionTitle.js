import React from "react";

const ExchangeSectionTitle = ({title}) => {
  return (
    <div className="pt-8 px-[16px] mb-5 text-[16px] font-[700] left-6 text-[#5E6272]">
        {title}
    </div>
  );
};

export default ExchangeSectionTitle;